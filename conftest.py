#   This currently doesn't need to be at the top level: only tests under
#   src/ use this fixture. But on the other hand, it can be nice to
#   keep all conftest.py stuff at the top level for easy reference.

from    testmc.pytest  import * # t8dev test framework fixtures
